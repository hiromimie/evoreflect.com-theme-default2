# EvoReflect Default2 Theme

EvoReflect用のDefault2テーマです。Nordカラースキームにインスパイアされた落ち着いたブルーグレー系のダークテーマと、明るく視認性の高いライトテーマを提供し、レスポンシブデザインに対応しています。

## インストール

このテーマはEvoReflect本体の `src/themes/default2/` にサブモジュールとして含まれています。

```bash
# サブモジュールを含めてクローン
git clone --recursive https://github.com/hiromimie/evoreflect.com.git

# または既存のリポジトリでサブモジュールを初期化
git submodule update --init --recursive
```

## テーマ構造

```
evoreflect.com-theme-default2/
├── theme.json       # テーマ定義ファイル（JSON形式）
├── gui-theme.json   # GUIテーマ定義（オプション）
├── theme-v05.json   # トークンベーステーマ（v0.5.0形式）
├── README.md        # このファイル
└── LICENSE          # MITライセンス
```

## 機能

- **Light/Darkモード対応** (Issue #1121): ライトモードとダークモードを切り替え可能
- **レスポンシブデザイン対応**: モバイル、タブレット、デスクトップに最適化されたレイアウト
- **ハイブリッドテーマ**: CLI（terminal）とGUI（Web）の両方で使用可能

## カラーパレット

### ダークモード (Nord ベース)

| カラー | コード | 用途 |
|--------|--------|------|
| Primary | `#88C0D0` | メインアクセント（フロストブルー） |
| Secondary | `#81A1C1` | セカンダリアクセント（スチールブルー） |
| Accent | `#B48EAD` | 強調色（オーロラパープル） |
| Background | `#2E3440` | 背景色（ポーラーナイト） |
| Foreground | `#ECEFF4` | 前景色（スノーストーム） |
| Success | `#A3BE8C` | 成功表示（オーロラグリーン） |
| Error | `#BF616A` | エラー表示（オーロラレッド） |
| Warning | `#EBCB8B` | 警告表示（オーロライエロー） |

### ライトモード (Nord Light ベース)

| カラー | コード | 用途 |
|--------|--------|------|
| Primary | `#5E81AC` | メインアクセント（フロストダークブルー） |
| Secondary | `#88C0D0` | セカンダリアクセント（フロストブルー） |
| Accent | `#B48EAD` | 強調色（オーロラパープル） |
| Background | `#ECEFF4` | 背景色（スノーストーム） |
| Foreground | `#2E3440` | 前景色（ポーラーナイト） |
| Success | `#A3BE8C` | 成功表示（オーロラグリーン） |
| Error | `#BF616A` | エラー表示（オーロラレッド） |
| Warning | `#D08770` | 警告表示（オーロラオレンジ） |

## レスポンシブデザイン

このテーマは、以下のブレークポイントで最適化されています：

| デバイス | ブレークポイント | 説明 |
|---------|----------------|------|
| Mobile | `< 768px` | スマートフォン向け |
| Tablet | `768px - 1439px` | タブレット向け |
| Desktop | `>= 1440px` | デスクトップ向け |

### レスポンシブ機能

- **サイドバー自動折りたたみ**: モバイル・タブレットでは自動的にサイドバーが折りたたまれます
- **可変フォントサイズ**: 画面サイズに応じてフォントサイズが最適化されます
- **フレキシブルレイアウト**: チャットエリアやコンテンツ幅が画面サイズに応じて調整されます

## ライセンス

MIT License
