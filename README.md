# EvoReflect Default2 Theme

EvoReflect用のDefault2テーマです。Blue/Warmデュアルパレットを採用し、ダークモードでは深い青系の落ち着いた配色、ライトモードでは温かみのあるアンバー系の配色を提供します。レスポンシブデザインに対応しています。

## インストール

このテーマはEvoReflect本体の `src/themes/default2/` にサブモジュールとして含まれています。

```bash
# サブモジュールを含めてクローン
git clone --recursive https://github.com/hiromimie/evoreflect.com.git

# または既存のリポジトリでサブモジュールを初期化
git submodule update --init --recursive
```

## テーマ構造

```
evoreflect.com-theme-default2/
├── theme.json       # テーマ定義ファイル（JSON形式）
├── gui-theme.json   # GUIテーマ定義（オプション）
├── theme-v05.json   # トークンベーステーマ（v0.5.0形式）
├── avatar/          # アバター画像（Issue #1189）
│   ├── user.svg     # ユーザーアバター
│   └── assistant.svg # アシスタントアバター
├── README.md        # このファイル
└── LICENSE          # MITライセンス
```

## 機能

- **Light/Darkモード対応** (Issue #1121, #1125): ライトモードとダークモードを切り替え可能
- **レスポンシブデザイン対応**: モバイル、タブレット、デスクトップに最適化されたレイアウト
- **ハイブリッドテーマ**: CLI（terminal）とGUI（Web）の両方で使用可能
- **デュアルパレット設計**: ダーク/ライトモードで独立したカラーパレット
- **カスタムアバター** (Issue #1189): テーマカラーに合わせたSVGアバター画像を同梱

## カラーパレット

### ダークモード (Blue パレット)

| カラー | コード | 用途 |
|--------|--------|------|
| Primary | `#9DAEE1` | メインアクセント（ライトブルー） |
| Secondary | `#637ECF` | セカンダリアクセント（ミディアムブルー） |
| Accent | `#F2952C` | 強調色（ウォームオレンジ） |
| Background | `#121C3B` | 背景色（ディープネイビー） |
| Foreground | `#E8ECF6` | 前景色（ライトブルーホワイト） |
| Success | `#7EC89D` | 成功表示（グリーン） |
| Error | `#E87070` | エラー表示（レッド） |
| Warning | `#F6B974` | 警告表示（ウォームアンバー） |

### ライトモード (Warm パレット)

| カラー | コード | 用途 |
|--------|--------|------|
| Primary | `#C56F0D` | メインアクセント（バーントオレンジ） |
| Secondary | `#F2952C` | セカンダリアクセント（ブライトオレンジ） |
| Accent | `#3654B0` | 強調色（ブルー） |
| Background | `#FBDDBC` | 背景色（ライトピーチ） |
| Foreground | `#3D2200` | 前景色（ダークブラウン） |
| Success | `#4D8030` | 成功表示（ダークグリーン） |
| Error | `#C43030` | エラー表示（ダークレッド） |
| Warning | `#C56F0D` | 警告表示（オレンジ） |

### コアパレット

**ダークモード基本色**:
`#9DAEE1` → `#637ECF` → `#3654B0` → `#243875` → `#121C3B`

**ライトモード基本色**:
`#FBDDBC` → `#F6B974` → `#F2952C` → `#C56F0D` → `#7D4608`

## レスポンシブデザイン

このテーマは、以下のブレークポイントで最適化されています：

| デバイス | ブレークポイント | 説明 |
|---------|----------------|------|
| Mobile | `< 768px` | スマートフォン向け |
| Tablet | `768px - 1439px` | タブレット向け |
| Desktop | `>= 1440px` | デスクトップ向け |

### レスポンシブ機能

- **サイドバー自動折りたたみ**: モバイル・タブレットでは自動的にサイドバーが折りたたまれます
- **可変フォントサイズ**: 画面サイズに応じてフォントサイズが最適化されます
- **フレキシブルレイアウト**: チャットエリアやコンテンツ幅が画面サイズに応じて調整されます

## ライセンス

MIT License
